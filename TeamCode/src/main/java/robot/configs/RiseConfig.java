package robot.configs;

public class RiseConfig extends RobotConfig {
    {
        define(
                mecanumDrive,
                lift,
                carousel,
                intake,
                gyroSensors,
                outtake,
                colorSensors,
                odometry
        );
    }
}
